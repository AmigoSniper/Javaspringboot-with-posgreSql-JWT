package com.pwl.tokosaya.repo;

import com.pwl.tokosaya.model.Penjual;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PenjualRepo extends JpaRepository<Penjual, Long> {
}
